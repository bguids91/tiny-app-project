<!DOCTYPE html>
<html lang="en">

<head>
</head>

<body>

  <header>
    <%- include partials/_header %>
  </header>


  <h2>
    Your URLS
  </h2>

  <ul>
    <% for(let key in urls) { %>
    <li>
    <a href="/u/<%= [key] %>"><%= [key] %></a> : <a href="<%= urls[key].longURL %>"><%= urls[key].longURL %></a>
      <a href="/urls/<%= [key] %>">Edit</a>
      <form method="POST" action="/urls/<%= [key] %>/delete"><button type="submit">Delete</button></form>
      <% } %>
    </li>
  </ul>


<h3>
  <a href="/urls/new">Add a new URL</a>
</h3>

</body>


</html>
